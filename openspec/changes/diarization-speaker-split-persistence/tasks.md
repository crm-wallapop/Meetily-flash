## 1. Repository split-and-persist (red→green)

- [x] 1.1 Write failing test `persist_aligned_splits_replaces_source_with_n_rows` — given a source row id and two `AlignedSegment`s with distinct speakers, asserting the source row is gone and two rows exist with split text + clamped timing + distinct `speaker_label`/`speaker_source='auto'` (non-regression; fails under any `UPDATE`-by-id scheme).
- [x] 1.2 Write failing test `persist_aligned_splits_overrides_and_copies` — assert each split row has `token_timestamps IS NULL`, `duration` derived from its own clamped [start,end] (not the source's), a fresh UUID id distinct from the source and siblings, and **every non-overridden column equal to the source**. Enumerate columns DYNAMICALLY via `PRAGMA table_info(transcripts)` against an override-exclusion set `{id, transcript, audio_start_time, audio_end_time, speaker_label, speaker_source, duration, token_timestamps}` (do NOT hard-code the column list — a static enumeration would silently miss a future migration column AND currently misses `previous_label` from migration 20260609).
- [x] 1.3 Write failing test `persist_aligned_splits_single_segment_updates_in_place` — N=1 keeps the same row id, sets BOTH `speaker_label` AND `speaker_source='auto'`, no delete/insert.
- [x] 1.4 Write failing test `persist_aligned_splits_skips_manual_source` — a source row with `speaker_source='manual'` is left untouched.
- [x] 1.5 Write failing test `persist_aligned_splits_prompt_injection_text_survives_verbatim` — prompt-injection payload persisted as data, bound via sqlx `?`.
- [x] 1.6 Write failing test `persist_aligned_splits_sql_meta_chars_survive_and_table_intact` — payload `'; DROP TABLE transcripts; --` is split as ordinary text AND the `transcripts` table still exists and is queryable after (distinct §4 category from prompt injection).
- [x] 1.7 Write failing test `persist_aligned_splits_proportional_tail_no_overlap` — when no diarization segment overlaps the source range, the row is labeled "Unknown Speaker" with its own timing, and no row has `audio_start_time > audio_end_time`.
- [x] 1.8 Write failing property-based test `persist_aligned_splits_invariants` — over arbitrary (source_range, diarization_segments) layouts, assert ALL of: (a) word conservation per path, (b) the union of split-row time-ranges is a SUBSET of the source span (⊆, NOT equality — diarization segments have internal gaps that no split row covers, so equality is unsatisfiable and would false-fail a correct implementation), (c) `duration == end - start` per row, (d) monotonic ordering, (e) no empty-text rows, (f) each row's speaker is one of {diarization speakers overlapping that row, "Unknown Speaker"}, (g) `audio_start_time <= audio_end_time`. The generator MUST produce layouts with internal gaps (the common case). Exercise BOTH token and proportional paths.
- [x] 1.9 Write failing test `persist_aligned_splits_cjk_no_whitespace` — CJK/no-whitespace text spanning two speakers is divided by character ratio (not dumped 100% to one speaker because `words.len()==1`).
- [x] 1.10 Write failing test `persist_aligned_splits_malformed_source_columns` — source row with `duration<=0` / NULL `meeting_id` / `audio_start_time > audio_end_time` is handled (skipped or carried as-is) without panicking.
- [x] 1.11 Write failing test `persist_aligned_splits_malformed_token_timestamps_json` — malformed JSON in `token_timestamps` is handled as if tokens were unavailable (proportional fallback) or the row skipped; no panic, no partial write.
- [x] 1.12 Write failing test `persist_aligned_splits_empty_diarization` — empty or all-"Unknown" diarization produces a single in-place row labeled "Unknown Speaker".
- [x] 1.13 Write failing test `persist_aligned_splits_oversized_row_and_large_n` — a ~500 kB source row splits without OOM, and N=120 splits persist without error. **The implementation uses per-row INSERTs (~15 host params each), so no single statement approaches the SQLite per-statement ceiling regardless of N — no multi-row VALUES or chunking is needed.** The original task text proposed chunked inserts at the ceiling; per-row inserts keep the bound constant, making chunking unnecessary (amended during code review).
- [x] 1.14 Write failing test `persist_aligned_splits_transaction_atomicity` — inject a failure between the source-row delete and the Nth insert; assert the transaction rolled back (source row unchanged, or all N splits present — never a partial delete). Injection mechanism: use deterministic UUIDs for the test (not random), and a fail-after-N executor wrapper (a test-only `SpeakerRepository` trait double whose `insert` panics on the Nth call), so the failure point is reproducible. Confirm the implementation issues the delete + N inserts as ONE multi-statement sqlx transaction (not N separate calls), so the rollback is atomic.
- [x] 1.15 Implement `SpeakerRepository::persist_aligned_splits(pool, meeting_id, source_row, splits)` in `speaker.rs`: transactional delete-source + insert-N for N>1 (copy-all-except-overrides; chunked inserts at the host-param ceiling); in-place `UPDATE` of `speaker_label`+`speaker_source='auto'` for N==1; skip if source `speaker_source='manual'`; "Unknown Speaker" in-place fallback when splits is empty. Make 1.1–1.14 pass.
- [x] 1.16 Fix the `align_proportional` tail at `alignment.rs:228-243` so the no-overlap case falls back to "Unknown Speaker" with the source row's own timing (no foreign-speaker borrow, no `start>end` row); fix the CJK whitespace assumption in the proportional word-split. Make 1.7, 1.8, 1.9 pass.

## 2. Wire both use cases to the shared routine

- [x] 2.1 Write failing integration test asserting `run_diarization_for_meeting` produces N persisted rows for a 2-speaker source segment (mock adapter emitting two speaker segments across one transcript row).
- [x] 2.2 Replace the per-`AlignedSegment` `update_transcript_speaker` loop in `run_diarization_for_meeting` (`commands.rs:531-539`) with grouping of `AlignedSegment`s by `original_id` and one `persist_aligned_splits` call per source row. Make 2.1 pass.
- [x] 2.3 Apply the same grouping + `persist_aligned_splits` replacement in `DiarizationProcessor::process` (`diarization_processor.rs:186-193`). Note in a code comment that this path is currently dead in production (queue wired with `None`) but fixed defensively. **Unit test mirroring 2.1 is deferred to `hexagonal-port-traits`** — `process()` decodes audio inline (`decode_audio_file`, not behind `DiarizationPort`), so a mock-port test still requires a real audio file + pool, i.e. an integration test, not a unit test. The wiring itself delegates to the same `persist_aligned_groups` helper covered by 2.1, so the persist contract is exercised.

## 3. Re-diarization idempotency + concurrency

- [x] 3.1 Write failing test `rediarize_is_idempotent_strict` — diarize a meeting twice; assert (a) the second pass does NOT re-expand any previously-split row (each fine row → N=1 in-place relabel), (b) the meeting's transcript row **id-set after == id-set before** (strict equality), (c) every split row still has `token_timestamps IS NULL`. Relies on 1.2's NULL-tokens invariant. (NOT a "row count non-decreasing" assertion — that would pass the exact NULL-tokens regression this test must catch.)
- [x] 3.2 Write failing test `rediarize_mutual_exclusion_per_meeting` — two concurrent diarize invocations on the same `meeting_id` are serialized by a meeting-level guard; the persisted result reflects one consistent pass, not an interleaving.
- [x] 3.3 Write failing test `persist_aligned_splits_distinct_sources_correct_under_serialized_writes` — two persisted splits on distinct source rows do not corrupt each other. **Renamed during code review:** SQLite's in-memory shared-cache returns SQLITE_LOCKED on truly concurrent writes (which busy_timeout cannot retry), so the test uses a single-connection pool and proves serialized row-correctness (disjoint split sets, source-id-scoped DELETEs), NOT SQLite-level isolation under true concurrency.

## 4. Frontend verification + smoke

- [x] 4.1 Write failing frontend unit test asserting the transcript renderer handles one source row becoming N rows (variable count) without assuming positional/stable keys — drive it with a fixture where a single row is replaced by two and assert both render. (Red-then-green, not an "audit".) Fix the renderer only if the test fails.
- [x] 4.2 Add `frontend/e2e/smoke/diarization-speaker-split-persistence.spec.ts` asserting the event-bus→UI wiring renders **distinct** speaker-badge text for a meeting whose diarization produced a multi-speaker split. Mock the `diarization-complete` event AND the transcripts re-fetch (the event payload carries no row data), emitting **before** any reload (post-reload `page.evaluate` emit is inert), then assert two rendered rows carry **different** badge text (not just two badges).

## 5. Pre-archive gate

- [x] 5.1 Run `cargo test`, `pytest backend/`, `pnpm test`, `pnpm lint`, `pnpm test:smoke` — all green.
- [x] 5.2 Re-read `openspec/specs/speaker-diarization/spec.md` and `openspec/specs/post-meeting-pipeline/spec.md` + this change's `design.md`; amend deltas if implementation evolved during apply.
- [ ] 5.3 `/opsx:archive` after spec/design reconciliation.
