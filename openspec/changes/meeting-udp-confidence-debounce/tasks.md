# Tasks — meeting-udp-confidence-debounce

Adversarial TDD: each task writes the failing (RED) test first, then the implementation that makes it pass. Run `CL=/FS cargo test --lib` from `frontend/src-tauri` (covers `detection::` and `meeting_detection::`). The design is the **locked-first-drop latch** (design.md D1–D4); `transient_observed` is deliberately NOT introduced — it is dead under D3's immutability requirement.

## 1. Adapter latch redesign (detection/windows.rs)

- [x] 1.1 RED `first_drop_after_window_latches_stable_true` — drive `bc=true` for ≥ `STABLE_CONFIDENCE_WINDOW` via the controllable clock, then one `bc=false` (no recovery); assert `stable_capture == true`. Fails on current code (the drop self-latches `false`).
- [x] 1.2 RED `flicker_during_debounce_does_not_flip_latch` (**self-heal guard, shark C1**) — `bc=true` (≥window) → `bc=false` (latch `Some(true)`) → `bc=true` (1-poll WASAPI flicker) → `bc=false`; assert `stable_capture == true` on EVERY post-drop poll and the latch was NOT cleared by the flicker. Load-bearing regression test (the recovery-based draft failed it).
- [x] 1.3 RED `short_stable_run_then_drop_is_15s` — `bc=true` for < window, then `bc=false`; assert `stable_capture == false`.
- [x] 1.4 RED `recovered_transient_after_window_still_exits_4s` (**the D1 relaxation**) — `bc=true` (≥window) → `false` (first drop) → `true` (recovery) → `true` (≥window) → `false` (second drop); assert `stable_capture == true` (latch held from the first drop).
- [x] 1.5 RED `latch_held_stable_across_consecutive_false_polls` — stable exit then several consecutive `bc=false` polls; assert every poll reports the same `stable_capture == true`.
- [x] 1.6 RED `notify_exit_clears_latch_for_next_call` — stable exit sets latch `Some(true)`; `notify_exit()`; next call's first drop with a short run → `stable_capture == false` (no inheritance).
- [x] 1.7 RED `rapid_leave_rejoin_within_wasapi_lag_no_spurious_drop` (shark C2) — `bc` reads `true` throughout (rejoin within release lag; no real `true→false` edge); assert no latch set, `bc_true_since` continuous.
- [x] 1.8 RED `device_disconnect_mid_call_classified_by_run_length` (shark C1 rewritten) — `bc=true` (≥window) → `false` (device lost, observation `is_turn_exit == false`) → `true` (reconnect) → `<window` → `false` (exit); assert two variants: ≥window-disconnect → `stable_capture == true`, <window-disconnect → `stable_capture == false`; and the disconnect drop did NOT set `is_turn_exit`.
- [x] 1.9 RED `mid_call_app_start_measures_short_run` (shark I2) — construct the detector with `bc=true` already active; immediate `bc=false` → `run_len ≈ 0 < window` → `stable_capture == false` (conservative).
- [x] 1.10 RED `window_boundary_pins_gte` (shark I4) — `run_len` at exactly `STABLE_CONFIDENCE_WINDOW` → `true`; at window − ε → `false`; at window + ε → `true`.
- [x] 1.11 RED `fresh_detector_after_crash_has_no_inherited_latch` (shark C3) — construct a fresh `WindowsMeetingDetector` after a simulated mid-debounce crash (no `notify_exit`); assert `exit_stable_latch`/`bc_true_since` start `None`.
- [x] 1.12 GREEN implement the locked-first-drop latch. Per-call fields on `WindowsMeetingDetector`: `bc_true_since: Option<Instant>` (set on a `false→true` edge and on first poll if `bc==true`, stamped to the detector's start instant; cleared to `None` on a `true→false` edge **after** reading `run_len`) and `exit_stable_latch: Option<bool>` (set ONCE on the FIRST `true→false` drop to `Some(run_len >= STABLE_CONFIDENCE_WINDOW)`; once `Some(v)`, **immutable until `notify_exit()`** — not cleared by recovery, not recomputed). Emit `stable_capture = exit_stable_latch.unwrap_or(false)` on every poll. Add `pub const STABLE_CONFIDENCE_WINDOW: Duration = Duration::from_secs(20);`. Do NOT introduce `transient_observed`.
- [x] 1.13 GREEN reset `exit_stable_latch = None` and `bc_true_since = None` in `notify_exit()`.
- [x] 1.14 Inject a **comprehensive** clock seam (shark I3) — divert EVERY `Instant::now()` read inside `current_state()` (including `connection_first_seen_at`, the `last_bc` transition block, and the new `bc_true_since`) through one test-overridable provider; production uses `Instant::now()`. A partial seam is unsound.
- [x] 1.15 RED proptest (shark I5 + round-2 I-R2) — invariant: once `exit_stable_latch == Some(v)` it stays `Some(v)` until `notify_exit()`, and `Some(v) ⟹ ((run_len at the first drop) >= STABLE_CONFIDENCE_WINDOW) == v`. **Generator (non-vacuous — a naive bool generator almost never yields a ≥ window run at 2 s granularity):** generate `Vec<(bool, Duration)>` with `true`-run lengths biased toward 0–30 s (straddling the 20 s boundary); advance the injected clock by the 2 s poll interval between polls; insert `notify_exit()` at random segment boundaries so multiple calls' first-drops are exercised. Without the bias + segmentation the property is trivially satisfied by an impl that never latches.
- [x] 1.16 RED `first_poll_false_then_stable_run_measured_from_edge` (shark round-2 I-R1) — the detector's FIRST poll reads `bc == false` (app started before the browser opens `getUserMedia`), then `bc == true` for ≥ `STABLE_CONFIDENCE_WINDOW`, then `bc == false`; assert `stable_capture == true` (run measured from the `false → true` edge, not detector start — `bc_true_since` was `None` through the initial `false` polls). Sole cover of the `None → false` first-poll path; every other §1 test starts `bc == true` on poll 1.

## 2. Remove the unreachable-path tests/assertions

- [x] 2.1 Replace the tests that encode the old drop-edge `bc_drop_observed_this_call` latch — `stable_call_sets_stable_capture_true`, `device_disconnect_mid_call_latches_unstable_without_confusing_leave`, `rapid_leave_rejoin_within_wasapi_lag_keeps_capture_stable`, `first_bc_drop_latches_stable_capture_false`, and `notify_exit_resets_bc_drop_latch_for_next_call` (shark M3 — the last two also encode the old latch) — with the §1 drop/flicker-driven tests. Keep `turn_latch_survives_bc_drop_during_exit` (TURN path, unchanged by this proposal).
- [x] 2.2 Rewrite `stable_capture_true_drives_short_debounce_to_ended` to drive the REAL adapter latch through a drop sequence and KEEP it as a fast deterministic loop test (shark I6 — do NOT move timing to `#[ignore]` only). **Post-review relocation (shark round: test-bed soundness):** the test lives in `detection/windows.rs`'s `#[cfg(test)]` module, NOT `fake.rs` — `fake.rs` is `dev-detector`-gated (non-default), so a test there is invisible to `cargo test --lib`, defeating I6. Keep `stable_capture_false_holds_ended_through_long_debounce` (the 15 s path; stays in `fake.rs` as it drives the fake adapter). **Hard dependency on task 1.14 (round-2 I-R3):** the test exercises the REAL `WindowsMeetingDetector` via its `#[cfg(test)]` detector-probe seam + the task-1.14 injected clock, so 1.14 MUST land first.

## 3. Use-case wiring (use_cases/meeting_detection.rs)

- [x] 3.1 No logic change required — `step_detector` already reads `observation.stable_capture`. Update the comment at the debounce-selection site AND the `DetectorObservation::stable_capture` doc comment in `ports/meeting_detector.rs` to describe the locked-first-drop + immutability semantics (shark M8). Replace the stale "no bc drop observed this call" wording.
- [x] 3.2 RED + confirm `step_detector_stable_capture_drives_4s_when_latched` — exercise the InCall branch with `stable_capture=true` held across multiple polls; assert the 4 s debounce selection is stable (per-poll recompute is safe given §1.5's immutability).

## 4. End-to-end + smoke

- [x] 4.1 `#[ignore]` real-clock adapter test: a stable exit fires `meeting-ended` within ~5–6 s (4 s debounce + poll slack), not ~16 s. Runs via `cargo test -- --ignored`.
- [x] 4.2 Confirm the existing `meeting-auto-detect` smoke spec still passes (it asserts the event→UI wiring for `meeting-ended`, which is unchanged — the event name and payload are identical; only Rust-internal timing changed). No new smoke spec needed: per CLAUDE.md §3 the event→UI wiring is verified unaffected rather than assumed.

## 5. Spec sync + archive

- [ ] 5.1 Before archive: re-read the canonical `openspec/specs/meeting-auto-detect/spec.md` and this change's `design.md` + delta `specs/meeting-auto-detect/spec.md`; if implementation diverged (e.g. final window value), amend the delta spec and design first. (Canonical is already in `# Capability Spec` / `## Purpose` / `## Requirements` format — low archive-corruption risk per shark review.)
- [ ] 5.2 `CL=/FS cargo test --lib` green (unit + the new adversarial set); `pnpm test` + `pnpm test:smoke` green.
- [ ] 5.3 `/opsx:archive meeting-udp-confidence-debounce` (syncs the delta into the canonical spec).
