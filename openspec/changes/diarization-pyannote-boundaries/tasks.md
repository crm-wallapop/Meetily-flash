## Prerequisite

- [ ] 0.1 Confirm `diarization-speaker-split-persistence` is archived and the split-and-persist path is on `main`. This change's finer boundaries produce extra N>1 splits that depend on that path.

## 1. Pre-splitter integration (red→green)

- [ ] 1.1 Write failing test `adapter_config_consumes_segmentation_model_path` — assert at least one sherpa-onnx config object in the constructed pipeline references the segmentation model path, AND that swapping the model for a COMMITTED dummy fixture changes the diarizer's behavior in a specific, deterministic way (construction error or distinct segmentation output — not a flaky live-filesystem mutation; mark `#[ignore]` if it needs the real model). Red test for the ADDED "pyannote segmentation model is actually consumed" requirement; the phantom load-or-skip at `sherpa_adapter.rs:103-106` is gone.
- [ ] 1.2 Write failing test `presplitter_fastclustering_threshold_is_zero` — assert the constructed config is `FastClusteringConfig { num_clusters: -1, threshold: 0.0 }` with `min_duration_on: 0.0` and `min_duration_off: 0.0`. (`threshold` is a cosine-DISSIMILARITY cutoff where smaller → more clusters; 0.0 maximally fragments. A `threshold: 1.0` value would over-merge — the design's original error, falsified by panel + confirmed via sherpa-onnx source.)
- [ ] 1.3 Write failing test `presplitter_boundary_oracle_cde5c264` — on the real cde5c264 recording, assert a SPECIFIC boundary exists in the pre-splitter output AND is absent in the chunk-grid-only baseline, for TWO windows: (a) the Ricardo interjection at ≈46:58 and (b) the actual complaint window 5.7–32.5 s. Assert the specific boundary, not a window-wide count delta (the count delta could coincidentally align with the 2 s grid). (Real-audio harness; `#[ignore]` if the recording is absent.)
- [ ] 1.4 Write failing test `presplitter_sherpa_labels_isolated_by_deterministic_equivalence` — run the pipeline with sherpa's `speaker` field populated vs zeroed; assert the resulting `speaker_embeddings` registry is byte-identical. (The `speaker` field is an integer, not a string namespace, so a "no string leaks" assertion is non-falsifiable — deterministic equivalence is.)
- [ ] 1.5 Write failing test `presplitter_single_speaker_not_fragmented` — a single-speaker fixture yields exactly one cluster after AHC+smoothing (no spurious second cluster from the finer candidate boundaries).
- [ ] 1.6 Write failing test `presplitter_silent_audio_no_crash` — a silent/empty-audio fixture (pyannote emits nothing) yields zero chunks and returns an empty diarization result without panicking.
- [ ] 1.7 Implement the pre-splitter in `SherpaOnnxDiarizationAdapter::process()`: run `OfflineSpeakerDiarization` with `FastClusteringConfig { num_clusters: -1, threshold: 0.0 }`, `min_duration_on: 0.0`, `min_duration_off: 0.0` on the reused 16 kHz mono buffer, take each returned segment's `(start, end)` as a boundary, discard the `speaker` field, intersect boundaries with transcript-segment speech regions to form the chunk layout (REPLACING `build_chunks`'s `effective_split` uniform grid at `sherpa_adapter.rs:331`), then feed into the existing embedding + AHC + cap + temporal-smoothing + registry pipeline. Remove the phantom constructor load-or-skip. Make 1.1–1.6 pass.

## 2. Uniform shed-to-cap + turn-recovery (red→green)

- [ ] 2.1 Write failing test `presplitter_uniform_shed_recovers_turns` — the LOAD-BEARING hypothesis test. A long (≥45 min, or a synthetic fixture scaled past the cap) two-speaker meeting with a rapid-alternation region and a comparable monologue region: after uniform shed-to-cap + AHC + temporal-coherence smoothing, the alternation region recovers a threshold fraction (≥80%) of its within-region turns. This tests the design's core claim that turns are re-derived downstream, not carried by individual boundaries. If this test cannot be made green with uniform shedding, the fallback is embedding-delta-weighted shedding (keep high-delta boundaries preferentially) — re-open the design at that point.
- [ ] 2.2 Write failing test `presplitter_respects_chunk_cap_and_offbyone` — candidate-boundary counts at exactly `MAX_DIARIZATION_CHUNKS` (no shed) and at 601 (shed by one) both yield a chunk count ≤ the cap; and a stress fixture at ≈10× cap sheds without OOM.
- [ ] 2.3 Write failing test `presplitter_adversarial_audio_no_collapse` — noise/music that triggers many false change-points at `threshold: 0.0` does not collapse the meeting to one speaker (AHC+smoothing rejects the noise fragments) nor fragment a true single-speaker meeting.
- [ ] 2.4 Write failing test `presplitter_single_speaker_at_cap` — a long single-speaker meeting whose candidate boundaries hit the cap: AHC+smoothing re-coalesces all surviving fragments back to one speaker (the cap fragments do not become spurious speakers).
- [ ] 2.5 Implement uniform shed-to-cap in the pre-splitter: when the candidate-boundary count exceeds `MAX_DIARIZATION_CHUNKS`, shed every k-th boundary by position BEFORE embedding extraction (so Meetily embeds only the capped set), then merge sub-`MIN_SPEECH_SECS` survivors into their time-neighbor. Remove `build_chunks`'s `effective_split` uniform-grid subdivision (`sherpa_adapter.rs:331`) so the cap is enforced once. Make 2.1–2.4 pass.

## 3. Performance + reconciliation

- [ ] 3.1 Write failing perf test `presplitter_cde5c264_within_budget` — the pre-splitter path on cde5c264 completes within the `process()` time budget (the cached-AHC bound holds for the clustering half; the one-time pyannote decode + 2× embedding cost is the accepted overhead). If over budget, investigate Open Question 1 (downmixed/subset buffer for `process()`). (`#[ignore]` unless the on-disk recording is available.)
- [ ] 3.2 Run the existing diarization oracle tests on cde5c264 (the label-quality, temporal-coherence, and refine_pass2 oracles) with the pre-splitter enabled; assert no regression in label quality. If a regression appears, the threshold:1.0 over-segmentation or region-fair decimation is the prime suspect.

## 4. Pre-archive gate

- [ ] 4.1 Run `cargo test` (incl. `-- --ignored` for the cde5c264 boundary oracle and perf gate if the recording is available), `pytest backend/`, `pnpm test`, `pnpm lint` — all green. (No smoke test required: this change affects boundary placement in the diarization pass, not a new user-visible frontend behavior beyond what the companion change's smoke already covers.)
- [ ] 4.2 Re-read `openspec/specs/speaker-diarization/spec.md` + this change's `design.md`; amend the delta if implementation evolved (especially Open Questions 1 & 2 — the 2× embedding cost decision and the two-pass smart-decimation question).
- [ ] 4.3 `/opsx:archive` after spec/design reconciliation.
