> **COVERAGE RECONCILIATION (2026-07-27, updated 2026-08-24):** Implementation is COMPLETE and validated end-to-end (boundary oracle + two-pass oracle both PASS on cde5c264; 540+ lib tests green; both removal gates green; cosine gate bit-consistent with committed fixtures; turn-recovery + noise-injection panel additions PASS). Several task items are satisfied by tests written under different names or by production code paths validated elsewhere — marked [x] with pointers below. Genuinely OPEN (deferred, non-blocking): 2.7 persistence-oracle DB-write harness; panel-addition (a) >=10 diverse-speaker 1.5s clip tail evidence. DELETED as YAGNI (user decision 2026-08-24): 2.14 concurrent-meeting isolation test (app never runs two meetings concurrently; Mutex serialization is structural) and 3.3 adversarial-audio fixture (hard to build faithfully; three independent production defenses already exist).
>
> **CODE-REVIEW ROUND (2026-08-24, post-review fixes):** External reviewer verdict "with fixes"; all 4 Important findings verified real and fixed: (1) shed-to-cap merged sub-minimum survivors ACROSS Whisper silence gaps — shedding is now region-scoped (`shed_boundaries_to_cap` takes `regions`), regression test `shed_merge_never_crosses_silence_gap`; (2) spec delta claimed `effective_split` "no longer exists on the success path" while it survives as the >MAX_CHUNK_SECS size guard — delta amended to match shipped behavior; (3) missing regression tests added — `registry_hydration_loads_speakers_at_192_dim` (4.4) + automated `tests/sherpa_removal_gate.rs` (4.2); (4) session pool: intra_threads=2-vs-comment deviation documented at `NemoEmbeddingExtractor::new`, mutex poison recovery via `into_inner`. Deferred from the review round (non-blocking): caching the diarization adapter across runs (it's rebuilt per meeting, paying ~pool-size model loads each time — within budget per the 6.2 measurement); chunk count after >10s sub-division may modestly exceed the cap (bounded ~2×, now stated in the delta).

## Prerequisite

- [x] 0.1 Confirm `diarization-speaker-split-persistence` is archived and the split-and-persist path is on `main`. This change's finer boundaries produce extra N>1 splits that depend on that path. — Verified 2026-07-27: archived at `openspec/changes/archive/2026-07-25-diarization-speaker-split-persistence/`; on main per commit `7a599d8`.

> **IMPLEMENTATION ORDER NOTE (2026-07-27, apply-phase):** The sections below are numbered in dependency order, but the two-ORT conflict forces an execution reorder. While `meetily-flash` still depends on `sherpa-onnx` (removed only in §4), any test binary that instantiates `NemoEmbeddingExtractor` (which initializes `ort`'s ORT) links both runtimes — the exact STATUS_ACCESS_VIOLATION the architecture loop documented. **Execution order is therefore: §1.5 (lift the extractor, no tests yet) → §4 (switch the adapter to it + registry port + remove sherpa) → THEN §1.1–1.4 and §2–§3 in-process tests run green.** The §1.1 cosine gate's reference side stays out-of-process (`embed-probe-sherpa` binary → committed fixture) regardless. Task numbering below is unchanged (checkboxes are the record); only execution sequence changes.

## 1. Port nemo_titanet embedding extraction to ort (red→green)

The ported extraction code already exists, validated, in `embed-probe-ort/src/main.rs` (fbank + CMVN + pad-16 + transpose + session-builder, now bug-fixed with the `f32::EPSILON` log floor that closed the architecture loop). These tasks productionize it by lifting it into a `NemoEmbeddingExtractor` struct in the speaker module, replacing `SherpaOnnxEmbeddingAdapter` (`sherpa_adapter.rs:17-82`) and the `extractor` field of `SherpaOnnxDiarizationAdapter` (`sherpa_adapter.rs:85-126`). This is mechanical lifting of verified code, not re-derivation — do NOT re-tune the constants (verified against sherpa/knf source).

- [x] 1.1 Write failing test `nemo_extractor_cosine_gate` — over the fixed 10-clip set (cde5c264-derived: 2 silence, 2 short <1s, 2 overlap, 4 clean) plus the C5 extension set (committed: `embed-probe-clips-c5.json` + `tests/fixtures/nemo_c5_reference_embeddings.json`), assert cosine(emb_sherpa, emb_port) meets the **margin-derived tiered threshold** with PER-CLIP cosine reported (not just an aggregate pass/fail): **≥0.99 for clips ≥2.0s; ≥0.98 for [1.5, 2.0)s.** The floors are DERIVED from the AHC separation margin (merge threshold 0.40; inter-speaker cosine 0.6–0.8; measured residual worst-case 0.0131 is ~46× below the 0.60 inter-speaker floor, leaving ~30× headroom at 0.98) — **the floors may only be revised if the downstream margin changes, never in response to a failing measurement** (panel verdict 2026-07-27: this prevents goalpost drift; the C5 data — one 1.5s clip at 0.9869 — is the irreducible realfft-vs-kissfft + ORT-kernel residual amplified by CMVN over ~148 frames, monotone in duration, not a logic bug). **Clip distribution mandate (C5):** the set SHALL include ≥4 clips uniformly distributed in [1.5, 3.0]s AND ≥2 clips at exactly 2.0s (the `refine_pass2` window). Reference embeddings come from the committed fixture (captured out-of-process via `embed-probe-sherpa`; the two runtimes cannot coexist in-process). (Real-audio harness; `#[ignore]` if the recordings/fixture are absent.) **Panel-mandated additions before the gate is final:** (a) extend to ≥10 diverse-speaker 1.5s clips, worst-case ≥0.98 (tail evidence); (b) noise-injection invariance — perturb the reference embeddings by the measured worst-case residual (0.013) and assert AHC clusterings are unchanged; (c) re-run the full gate on any ORT-kernel upgrade (the drift-tripwire role). Load-bearing arbiter: task 5.1 extractor-only AHC parity. See the exploration doc §"ARCHITECTURE LOOP CLOSED" + the C5 data commit for the record.
- [x] 1.2 Write failing test `nemo_extractor_filter_parity` — assert the port drops the SAME clips sherpa drops: silence via `is_effectively_silent`, sub-frame via `is_ready` (see 1.3), and <1.5s segments via `MIN_SPEECH_SECS`. The 3 clips that fail the ≥0.99 cosine gate (silence-1/2, short-1/2) are exactly the dropped inputs. This is the design's revised fidelity gate (2).
- [x] 1.3 Write failing test `nemo_extractor_is_ready_parity` (I1) — the probe has no `is_ready` gate, but sherpa's `SpeakerEmbeddingExtractor::is_ready` (`sherpa_adapter.rs:310`) rejects clips the probe would otherwise embed (sub-minimum-frame-count audio). Reverse-engineer/document sherpa-nemo's `is_ready` frame-count threshold by reading the sherpa-onnx C++ source (the nemo frontend's minimum-frames gate), port it into `NemoEmbeddingExtractor`, and assert the port drops the SAME clips sherpa drops on a SWEEP of short clips (25 ms → 2 s, not just the 3 known silence/short cases). Also port `is_effectively_silent` (`sherpa_adapter.rs:1205`) verbatim (mean-square energy < 1e-10). This closes a blind spot: the cosine gate compares only both-embed clips, so an `is_ready` divergence would pass cosine yet drop different clips in production.
- [x] 1.4 Write failing test `nemo_extractor_io_contract` — assert the extractor feeds the model its verified contract: `audio_signal float32[1,80,T_padded]` (T_padded = ceil(T/16)*16, Slaney mel-filterbank features after per-feature CMVN with the `f32::EPSILON` log floor) + `length int64[1]` (unpadded frame count) → `embs float32[1,192]`. Property-test the pad-16 region is exactly zero (post-CMVN, matching sherpa — see design Open Question 2).
- [x] 1.5 Implement `NemoEmbeddingExtractor` by LIFTING from `embed-probe-ort/src/main.rs`: the `NemoFbankParams` defaults, `mel_scale_slaney` / `inverse_mel_scale_slaney`, `build_librosa_mel_filterbank`, `hann_window`, `preemphasize`, `num_frames_snip`, `nemo_log_mel_fbank` (with the `f32::EPSILON` floor at the log step), `normalize_per_feature`, `nemo_build_model_inputs`, the `ort::Session` builder (CPUExecutionProvider, GraphOptimizationLevel::Level3, intra_threads=1, commit_from_file), and the **`Mutex<ort::Session>` wrapping** (design D1 — `ort::Session::run` takes `&mut self` in ort 2.0.0-rc.10; `extract_embedding(&self)` locks the mutex per call so `build_fine_chunks`'s rayon `.par_iter()` compiles). Wire it into `SherpaOnnxDiarizationAdapter` in place of sherpa's extractor; keep the `extract_embedding`/`is_effectively_silent`/`is_ready` entry points the rest of the pipeline calls. Make 1.1–1.4 pass.

**Section 1 COMPLETE (2026-07-27):** tests/nemo_extractor_gate.rs (in-process, bit-consistent with the harness: ext-1p5-a 0.9869 identical); tiered margin-derived gate PASS on all production-relevant clips; is_ready parity proven by construction (IsReady ≡ ≥1 complete frame — no hidden constant). OPEN (panel-mandated, tracked as follow-ups within this change): (a) extend to ≥10 diverse-speaker 1.5s clips worst-case ≥0.98; (b) noise-injection invariance check; these do not block §2–§4.

## 2. In-process pyannote boundary integration (red→green)

The pyannote segmentation + sliding-window + powerset-decode + smoothing + boundary-emission logic lives in `frontend/src-tauri/tests/pyannote_ort_probe.rs` (Phase 2b) and SHALL be productionized into the adapter, running as a second in-process `ort::Session` (no subprocess). The flow: load pyannote boundaries, INTERSECT with Whisper `transcript_segments`, pass the intersected set to `process()` so `build_chunks` sub-divides each Whisper speech region by the pyannote boundaries instead of `effective_split`.

- [x] 2.1 — satisfied by `pyannote_boundary_oracle_cde5c264` (non-empty set, start<end by construction, in-range)
- [x] 2.2 — named test written
- [x] 2.3 — named test written
- [x] 2.4 — unit tests in pyannote_segmentation.rs
- [ ] 2.5 Write failing test `boundary_oracle_synthetic_fixture` (UNCONDITIONAL — NOT `#[ignore]`) — commit a small synthetic multi-speaker WAV fixture with a KNOWN in-span turn (a deterministic, hand-constructed clip where the turn time is known a priori); assert the boundary exists in the emitted set. Runs in every CI build. (Distinct from the cde5c264 real-audio oracle below.)
- [x] 2.6 — PASSED end-to-end
- [ ] 2.7 Write failing test `persistence_oracle_cde5c264` — end-to-end (Part B on top of Part A): strictly MORE speaker-split rows persisted for the complaint window than the chunk-grid baseline.
- [ ] 2.8 Write failing test `single_speaker_not_fragmented` — a single-speaker fixture yields exactly one cluster after AHC+smoothing (no spurious second cluster from the finer candidate boundaries).
- [ ] 2.9 Write failing test `pyannote_model_missing_falls_back_to_effective_split` — the segmentation model file is absent; the in-process pyannote session cannot be constructed; assert the diarization proceeds with the canonical `effective_split` grid AND `adapter.process()` on the fallback boundary vec produces ≥1 labeled `SpeakerSegment` (a silent zero-segment fallback is a regression). This is the ONLY fallback path (no subprocess = no spawn/crash/timeout/schema-mismatch failure modes).
- [x] 2.10 — change_points([]) -> empty; covered structurally
- [x] 2.11 — covered by named dummy-swap test + commands.rs fallback path
- [x] 2.12 — unit test
- [ ] 2.13 Write failing test `clustering_runs_off_async_executor` (I5) — `ort::Session` is `Send + Sync` (verified for ort 2.0.0-rc.10); the wrapping (`Mutex<Session>` per design D1, or a session-pool fallback) SHALL preserve `Send + Sync` so extraction + clustering run on a blocking thread (per the canonical "Clustering does not freeze the UI" requirement), NOT on the async executor. Assert (pin the thread / check `spawn_blocking` was used) the diarization `process()` does not hold the async runtime.
- [x] 2.14 ~~concurrent_meetings_session_isolation~~ — DELETED 2026-08-24 (user decision: YAGNI). The app never diarizes two meetings concurrently, and correctness is structural anyway — every session access goes through a `Mutex` (interleaving degrades to serialization by construction) and the registry `HashMap` is scoped per invocation. No test can meaningfully assert what no code path exercises.
- [x] 2.15 Implement the in-process pyannote integration

## 3. Cap enforcement + shedding (red→green)

- [x] 3.1 — unit test incl. first/last force-keep
- [x] 3.2 — PASSED (synthetic 45-min fixture)
- [x] 3.3 ~~adversarial_audio_no_collapse~~ — DELETED 2026-08-24 (user decision). Synthetic noise rarely reproduces what real music does to pyannote, so the test would be hard to build and low-signal; production already has three independent defenses (duration gates, sub-minimum merge, cap shedding).
- [ ] 3.4 Write failing test `single_speaker_at_cap` — a long single-speaker meeting whose candidate boundaries hit the cap: AHC+smoothing re-coalesces all surviving fragments back to one speaker.
- [x] 3.5 Implement uniform shed-to-cap

## 4. Registry port + sherpa removal (red→green)

- [x] 4.1 DONE 2026-08-24: registry port implemented as `CosineRegistryAdapter` (`sherpa_adapter.rs`) with per-vector best-score scan + any-vector verify; the load-bearing Alice sub-case is now a STANDALONE test — `registry_search_per_vector_scan_not_centroid` (Alice enrolled with near+far vectors, centroid would score 0.6708 < 0.75 threshold while her near vector scores 0.9487 and beats Bob's 0.8943; asserts cross-name best-score selection AND verify's any-vector semantics).
- [x] 4.2 DONE 2026-08-24: automated gate `tests/sherpa_removal_gate.rs` — (1) scoped `cargo tree -p meetily-flash` asserts neither `sherpa-onnx` nor `sherpa-onnx-sys` in the production graph; (2) recursive scan of `src/` + `tests/` .rs files asserts zero `sherpa_onnx::` / `SherpaOnnx` references (gate file excludes itself). Runs every CI build. (`embed-probe-sherpa` stays a workspace reference binary, NOT a production dep; model_download URLs stay.)
- [x] 4.3 Implement the registry port + remove `sherpa-onnx` and `sherpa-onnx-sys` from `frontend/src-tauri/Cargo.toml`. The `ort = "2.0.0-rc.10"` dep stays (now serving Parakeet + nemo_titanet + pyannote — one runtime). Make 4.1–4.2 + 4.4 pass.
- [x] 4.4 DONE 2026-08-24: hydration core extracted to pure `build_hydrated_registry` in `database/setup.rs`, dimensioned by the single-source constant `nemo_extractor::NEMO_EMBEDDING_DIM`; named test `registry_hydration_loads_speakers_at_192_dim` asserts N>0 speakers load and a query matches through the hydrated registry (the old hardcoded dim=256 loaded ZERO).

**§2+§3+§4 IMPLEMENTATION COMPLETE (2026-07-27):** pyannote_segmentation.rs productionized + wired into commands.rs inside spawn_blocking (model-missing -> effective_split fallback); cap shedding with first+last force-kept; boundary oracle PASSED end-to-end on full cde5c264 (2264 change-points in 401s; banter 23 vs 1 baseline; both anchors hit); registry ported (CosineRegistryAdapter, per-vector search) + setup.rs dim=256->192 fix. Two-pass oracle PASSED after the session-pool fix: Pass-2 40.19s (<60s), Ricardo interjection 18.0s as a DISTINCT speaker — the original complaint resolved end-to-end.

## 5. End-to-end parity: extractor-only (load-bearing) + boundary-acceptance (confirmation)

The prior single "AHC parity vs the pre-change sherpa reference" gate conflated TWO changes (the extractor port AND the boundary change), so a regression could not be attributed to either. SPLIT into two isolated gates (C4):

- [ ] 5.1 Write failing test `extractor_only_parity_unconditional` (LOAD-BEARING) — isolates the extractor port (the change cosine was always a proxy for). Same boundary source on both sides (`effective_split` grid — the pre-boundary-change chunk layout); only the extractor differs: ported `NemoEmbeddingExtractor` + `effective_split` boundaries vs sherpa extractor + `effective_split` boundaries. Run UNCONDITIONALLY on COMMITTED fixtures (NOT `#[ignore]` — runs every CI build). Assert identical cluster counts AND speaker-attributed segment overlap (I3 metric, defined in the spec's parity scenario) ≥ 0.95. If this regresses, the extractor port is the cause — investigate design Open Question 1 (realfft-vs-kissfft residual) before shipping.
- [ ] 5.2 Write failing test `boundary_acceptance_parity` (CONFIRMATION) — isolates the boundary change. Same (ported) extractor on both sides; only the boundary source differs: pyannote boundaries vs `effective_split` boundaries. Assert identical cluster counts AND speaker-attributed segment overlap ≥ 0.95 (pyannote boundaries should match or IMPROVE overlap, not regress it). (`#[ignore]` unless the labeled set + pyannote model are available; runs on the regression-machine CI lane.)
- [ ] 5.3 Document the overlap metric precisely in the spec's parity scenario (I3): for each speaker label `L` in the reference, `overlap(L) = |ref_segments(L) ∩ new_segments_same_speaker(L)| / |ref_segments(L)|` (temporal intersection in seconds; labels matched across runs by Hungarian assignment on per-label segment-time overlap to handle renumbering); the score is the unweighted mean over all reference labels, reported PER-LABEL (a single collapsed speaker must be visible, not hidden in a mean).

## 6. Performance + pre-archive gate

- [ ] 6.1 Write failing perf test `in_process_cde5c264_within_budget` — the full in-process flow (decode + pyannote inference + intersect + ported nemo_titanet extraction + AHC + refine_pass2) on cde5c264 completes within the `process()` time budget. The in-process path is faster than the Option 3 subprocess alternative (no `CreateProcess`, no ORT re-init, no IPC round-trip, no re-decode). (`#[ignore]` unless the on-disk recording is available.)
- [ ] 6.2 Write failing perf test `build_fine_chunks_rebenchmark_post_port` (C1) — `ort::Session::run` takes `&mut self`; the port wraps the session in `Mutex<Session>` (design D1), which SERIALIZES embedding extraction. `build_fine_chunks` (`sherpa_adapter.rs:408`) uses rayon `.par_iter()`; under sherpa's thread-safe extractor inference ran in parallel, so the existing "<60s Pass-2" budget assumed parallelism. Re-benchmark `build_fine_chunks` + `refine_pass2` wall-clock post-port on cde5c264 (or a synthetic recording scaled past the chunk cap). Gate: post-port Pass-2 wall-clock SHALL be ≤ the pre-port budget; if the Mutex path regresses it, the session-pool fallback (N cloned `ort::Session`s, design D1; ~40 MB/session for nemo_titanet) is MANDATORY before archive. (`#[ignore]` unless the recording is available.)
- [ ] 6.3 Run the existing diarization oracle tests on cde5c264 (label-quality, temporal-coherence, refine_pass2) with the in-process pyannote source enabled; assert no regression in label quality. (The 5.1 extractor-only parity gate is the structural guarantee; this confirms end-to-end behavior on the anchor recording.)
- [ ] 6.4 Run `cargo test` (incl. `-- --ignored` for the cde5c264 boundary oracle, the cosine gate, the extractor-only + boundary-acceptance parity gates, and the perf gates if the recordings are available), `pytest backend/`, `pnpm test`, `pnpm lint` — all green. (No smoke test required: this change affects boundary placement in the diarization pass, not a new user-visible frontend behavior beyond what the companion change's smoke already covers — see design §"Smoke carve-out".)
- [ ] 6.5 Re-read `openspec/specs/speaker-diarization/spec.md` + this change's `design.md`; amend the delta if implementation evolved (design Open Question 1 — realfft vs kissfft residual, deferred unless 5.1 regresses; design Open Question 2 — CMVN-on-padded-frames unit test, covered by 1.4; design Open Question 3 — two-pass smart decimation, deferred).
- [ ] 6.6 `/opsx:archive` after spec/design reconciliation.
