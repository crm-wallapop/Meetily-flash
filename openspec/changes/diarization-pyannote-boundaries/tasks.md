## Prerequisite

- [ ] 0.1 Confirm `diarization-speaker-split-persistence` is archived and the split-and-persist path is on `main`. This change's finer boundaries produce extra N>1 splits that depend on that path.

## 1. Port nemo_titanet embedding extraction to ort (red→green)

The ported extraction code already exists, validated, in `embed-probe-ort/src/main.rs` (fbank + CMVN + pad-16 + transpose + session-builder, now bug-fixed with the `f32::EPSILON` log floor that closed the architecture loop). These tasks productionize it by lifting it into a `NemoEmbeddingExtractor` struct in the speaker module, replacing `SherpaOnnxEmbeddingAdapter` (`sherpa_adapter.rs:17-82`) and the `extractor` field of `SherpaOnnxDiarizationAdapter` (`sherpa_adapter.rs:85-126`). This is mechanical lifting of verified code, not re-derivation — do NOT re-tune the constants (verified against sherpa/knf source).

- [ ] 1.1 Write failing test `nemo_extractor_cosine_gate` — over the fixed 10-clip set (cde5c264-derived: 2 silence, 2 short <1s, 2 overlap, 4 clean) plus production-representative additions, assert cosine(emb_sherpa, emb_port) ≥ 0.99 on EVERY clip ≥ 1.5s passing `is_effectively_silent` (the 6 production-clustering clips post-fix: 0.9946–0.9989). Reference embeddings come from the `embed-probe-sherpa` binary (the validated harness; the two runtimes cannot coexist in-process, so the reference is captured out-of-process and committed as a fixture). (Real-audio harness; `#[ignore]` if the recordings/fixture are absent.) This is the design's revised fidelity gate (1) — see `openspec/exploration/diarization-pyannote-boundaries-ort-probe.md` §"ARCHITECTURE LOOP CLOSED" for the post-fix table.
- [ ] 1.2 Write failing test `nemo_extractor_filter_parity` — assert the port drops the SAME clips sherpa drops: silence via `is_effectively_silent`, and <1.5s segments via `MIN_SPEECH_SECS` / the `< 1 mel frame` (is_ready=false) path. The 3 clips that fail the ≥0.99 cosine gate (silence-1/2, short-1/2) are exactly the dropped inputs. This is the design's revised fidelity gate (2).
- [ ] 1.3 Write failing test `nemo_extractor_i0_contract` — assert the extractor feeds the model its verified contract: `audio_signal float32[1,80,T_padded]` (T_padded = ceil(T/16)*16, Slaney mel-filterbank features after per-feature CMVN with the `f32::EPSILON` log floor) + `length int64[1]` (unpadded frame count) → `embs float32[1,192]`. Property-test the pad-16 region is exactly zero (post-CMVN, matching sherpa — see design Open Question 2).
- [ ] 1.4 Implement `NemoEmbeddingExtractor` by LIFTING from `embed-probe-ort/src/main.rs`: the `NemoFbankParams` defaults, `mel_scale_slaney` / `inverse_mel_scale_slaney`, `build_librosa_mel_filterbank`, `hann_window`, `preemphasize`, `num_frames_snip`, `nemo_log_mel_fbank` (with the `f32::EPSILON` floor at the log step), `normalize_per_feature`, `nemo_build_model_inputs`, and the `ort::Session` builder (CPUExecutionProvider, GraphOptimizationLevel::Level3, intra_threads=1, commit_from_file). Wire it into `SherpaOnnxDiarizationAdapter` in place of sherpa's extractor; keep the `extract_embedding`/`is_effectively_silent` entry points the rest of the pipeline calls. Make 1.1–1.3 pass.

## 2. In-process pyannote boundary integration (red→green)

The pyannote segmentation + sliding-window + powerset-decode + smoothing + boundary-emission logic lives in `frontend/src-tauri/tests/pyannote_ort_probe.rs` (Phase 2b) and SHALL be productionized into the adapter, running as a second in-process `ort::Session` (no subprocess). The flow: load pyannote boundaries, INTERSECT with Whisper `transcript_segments`, pass the intersected set to `process()` so `build_chunks` sub-divides each Whisper speech region by the pyannote boundaries instead of `effective_split`.

- [ ] 2.1 Write failing test `pyannote_session_emits_boundaries` — construct the in-process `ort::Session` over `pyannote-segmentation-3.0`, run it on a committed multi-speaker fixture (or `#[ignore]` on cde5c264), assert (a) a non-empty boundary set, (b) each boundary has `start < end`, (c) all boundaries within `[0, audio_duration]`. Red test for the "pyannote segmentation model is actually consumed" requirement.
- [ ] 2.2 Write failing test `pyannote_session_consumes_segmentation_model` — swap the model in the `model_dir` for a COMMITTED dummy fixture; assert the session's behavior changes deterministically (construction error or distinct/empty segmentation output — not a flaky live-filesystem mutation). Closes the phantom-dependency state.
- [ ] 2.3 Write failing test `pyannote_session_smooths_at_defaults` — assert the smoothing config is median rad=3, min_on=0.3s, max_off=0.5s at onset 0.5 (the only Phase 2b config that hit BOTH anchors). Inspect via a `--dump-config` dev flag or the progress log.
- [ ] 2.4 Write failing test `intersect_pyannote_with_whisper_segments` — at `commands.rs:413-432`, the flow INTERSECTS the pyannote boundary set with the Whisper `transcript_segments` (`fetch_transcript_timestamps`): pyannote boundaries inside a Whisper speech region are kept as intra-region splits; Whisper silence regions are preserved as silence (NOT embedded); the INTERSECTED set is passed as `transcript_segments` to `adapter.process()` (mock the session or use a fixture; assert `process()` receives exactly the intersected set, not the raw pyannote set).
- [ ] 2.5 Write failing test `boundary_oracle_synthetic_fixture` (UNCONDITIONAL — NOT `#[ignore]`) — commit a small synthetic multi-speaker WAV fixture with a KNOWN in-span turn (a deterministic, hand-constructed clip where the turn time is known a priori); assert the boundary exists in the emitted set. Runs in every CI build. (Distinct from the cde5c264 real-audio oracle below.)
- [ ] 2.6 Write failing test `boundary_oracle_cde5c264` (`#[ignore]` unless the on-disk recording is available) — on the real cde5c264 recording, assert a SPECIFIC boundary exists in the emitted set AND is absent in the chunk-grid-only baseline, for TWO windows: (a) the Ricardo interjection at ≈46:58 and (b) the actual complaint window 5.7–32.5 s. Assert the specific boundary, not a window-wide count delta. (Full-fidelity validation; the synthetic fixture above is the unconditional gate.)
- [ ] 2.7 Write failing test `persistence_oracle_cde5c264` — end-to-end (Part B on top of Part A): strictly MORE speaker-split rows persisted for the complaint window than the chunk-grid baseline.
- [ ] 2.8 Write failing test `single_speaker_not_fragmented` — a single-speaker fixture yields exactly one cluster after AHC+smoothing (no spurious second cluster from the finer candidate boundaries).
- [ ] 2.9 Write failing test `pyannote_model_missing_falls_back_to_effective_split` — the segmentation model file is absent; the in-process pyannote session cannot be constructed; assert the diarization proceeds with the canonical `effective_split` grid AND `adapter.process()` on the fallback boundary vec produces ≥1 labeled `SpeakerSegment` (a silent zero-segment fallback is a regression). This is the ONLY fallback path (no subprocess = no spawn/crash/timeout/schema-mismatch failure modes).
- [ ] 2.10 Write failing test `silent_audio_empty_boundary_set` — silent/empty audio fixture → the pyannote session emits an empty boundary set → the diarization proceeds (empty intersected set or effective-split fallback) without panicking.
- [ ] 2.11 Implement the in-process pyannote integration by PORTING from `pyannote_ort_probe.rs` (Phase 2b) into the speaker module: (a) a second `ort::Session` over `pyannote-segmentation-3.0` (the Phase 1 builder pattern, `pyannote_ort_probe.rs:48-59`), (b) sliding 10s window at 1s step, per-frame powerset decode to 3-speaker multilabel via hysteresis at onset 0.5, (c) smoothing (median rad=3 / on=0.3s / off=0.5s / onset 0.5), (d) boundary emission, (e) the intersect-with-Whisper-`transcript_segments` step at `commands.rs:413-432`, (f) the pyannote-model-missing → `effective_split` fallback. Modify `build_chunks` (`sherpa_adapter.rs:331`) to sub-divide each Whisper speech region by the pyannote boundaries INSTEAD of `effective_split`. Make 2.1–2.10 pass.

## 3. Cap enforcement + shedding (red→green)

- [ ] 3.1 Write failing test `sheds_to_chunk_cap` — candidate-boundary counts at exactly `MAX_DIARIZATION_CHUNKS` (no shed) and at 601 (shed by one) both yield a boundary count ≤ the cap passed to `adapter.process()`; a stress fixture at ≈10× cap sheds without OOM.
- [ ] 3.2 Write failing test `uniform_shed_recovers_turns` — the LOAD-BEARING hypothesis test. A long (≥45 min, or a synthetic fixture scaled past the cap) two-speaker meeting with a rapid-alternation region and a comparable monologue region: after the uniform shed + AHC + temporal-coherence smoothing, the alternation region recovers ≥80% of its within-region turns. If this cannot be made green with uniform shedding, the fallback is embedding-delta-weighted shedding (keep high-delta boundaries preferentially) — re-open the design at that point.
- [ ] 3.3 Write failing test `adversarial_audio_no_collapse` — noise/music that triggers many false change-points does not collapse the meeting to one speaker (AHC+smoothing rejects the noise fragments) nor fragment a true single-speaker meeting.
- [ ] 3.4 Write failing test `single_speaker_at_cap` — a long single-speaker meeting whose candidate boundaries hit the cap: AHC+smoothing re-coalesces all surviving fragments back to one speaker.
- [ ] 3.5 Implement uniform shed-to-cap at the pyannote-boundary layer (design D4/D5): when the candidate-boundary count exceeds `MAX_DIARIZATION_CHUNKS`, shed every k-th boundary by position BEFORE embedding; merge sub-`MIN_SPEECH_SECS` survivors into their time-neighbor. Remove `build_chunks`'s `effective_split` uniform-grid subdivision (`sherpa_adapter.rs:331`) so the cap is enforced once, at the boundary layer. Make 3.1–3.4 pass.

## 4. Registry port + sherpa removal (red→green)

- [ ] 4.1 Write failing test `registry_pure_rust_cosine_store` — replace `SherpaOnnxRegistryAdapter` (`sherpa_adapter.rs:1297-1362`) with an in-memory `HashMap<String, Vec<Vec<f32>>>` + cosine search using the existing `cosine_similarity` helper (`sherpa_adapter.rs:1191`); assert `add`/`search`/`verify` behave identically to the sherpa manager on the same `Vec<f32>` inputs (the on-disk `speaker_embeddings` schema is unchanged — nemo_titanet 192-dim either way).
- [ ] 4.2 Write failing test `sherpa_removed_from_dependency_graph` — the LOAD-BEARING removal gate. `cargo tree` on the workspace shows neither `sherpa-onnx` nor `sherpa-onnx-sys`; a grep for `sherpa_onnx::` in `frontend/src-tauri/src/` returns zero hits. (3 files touched per the design blast radius: `sherpa_adapter.rs`, `model_download.rs`, `commands.rs`. The pyannote + nemo_titanet download URLs in `model_download.rs` STAY — both models are still downloaded, only the loader changes.)
- [ ] 4.3 Implement the registry port + remove `sherpa-onnx` and `sherpa-onnx-sys` from `frontend/src-tauri/Cargo.toml`. The `ort = "2.0.0-rc.10"` dep stays (now serving Parakeet + nemo_titanet + pyannote — one runtime). Make 4.1–4.2 pass.

## 5. End-to-end AHC parity (the load-bearing gate)

- [ ] 5.1 Write failing test `ahc_parity_on_labeled_recordings` — the design's revised fidelity gate (3) and the gate cosine was always a proxy for. On ≥10 labeled multi-speaker recordings (the pre-change sherpa reference produced by the `embed-probe-sherpa` harness + committed reference outputs), assert (a) identical cluster counts and (b) ≥95% speaker-attributed segment overlap vs the reference, for the full pipeline (ported nemo_titanet extractor + in-process pyannote boundaries + the unchanged AHC + cap + smoothing + refine_pass2 + registry). If this regresses, investigate design Open Question 1 (realfft-vs-kissfft residual) before shipping. (`#[ignore]` unless the labeled set + reference outputs are available; runs on the regression-machine CI lane.)

## 6. Performance + pre-archive gate

- [ ] 6.1 Write failing perf test `in_process_cde5c264_within_budget` — the full in-process flow (decode + pyannote inference + intersect + ported nemo_titanet extraction + AHC + refine_pass2) on cde5c264 completes within the `process()` time budget. The in-process path is faster than the Option 3 subprocess alternative (no `CreateProcess`, no ORT re-init, no IPC round-trip, no re-decode). (`#[ignore]` unless the on-disk recording is available.)
- [ ] 6.2 Run the existing diarization oracle tests on cde5c264 (label-quality, temporal-coherence, refine_pass2) with the in-process pyannote source enabled; assert no regression in label quality. (The 5.1 AHC-parity gate is the structural guarantee; this confirms end-to-end behavior on the anchor recording.)
- [ ] 6.3 Run `cargo test` (incl. `-- --ignored` for the cde5c264 boundary oracle, the cosine gate, the AHC-parity gate, and the perf gate if the recordings are available), `pytest backend/`, `pnpm test`, `pnpm lint` — all green. (No smoke test required: this change affects boundary placement in the diarization pass, not a new user-visible frontend behavior beyond what the companion change's smoke already covers — see design §"Smoke carve-out".)
- [ ] 6.4 Re-read `openspec/specs/speaker-diarization/spec.md` + this change's `design.md`; amend the delta if implementation evolved (design Open Question 1 — realfft vs kissfft residual, deferred unless 5.1 regresses; design Open Question 2 — CMVN-on-padded-frames unit test, covered by 1.3; design Open Question 3 — two-pass smart decimation, deferred).
- [ ] 6.5 `/opsx:archive` after spec/design reconciliation.
