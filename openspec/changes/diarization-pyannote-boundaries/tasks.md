## Prerequisite

- [ ] 0.1 Confirm `diarization-speaker-split-persistence` is archived and the split-and-persist path is on `main`. This change's finer boundaries produce extra N>1 splits that depend on that path.

## 1. Child binary productionization (red→green)

The `embed-probe-ort/` crate exists from the panel's harness, but it is a TEMPLATE only: its `src/main.rs` loads `nemo-titanet-embedding.onnx` and emits per-clip EMBEDDINGS (a `Vec<f32>`), NOT pyannote boundaries. The pyannote segmentation + sliding-window + powerset-decode + smoothing + boundary-emission logic lives in `frontend/src-tauri/tests/pyannote_ort_probe.rs` (Phase 2b) and SHALL be ported from there into the child crate. This is substantial new code, not a thin wrapper — the existing crate contributes the `ort` session-builder pattern and IPC scaffolding only. These tasks productionize the new child crate.

- [ ] 1.1 Write failing test `child_binary_emits_pyannote_boundaries` — invoke the child binary on a committed multi-speaker fixture (or `#[ignore]` on cde5c264), parse its JSON stdout, assert (a) a non-empty `boundaries` array, (b) each boundary has `start < end`, (c) all boundaries within `[0, audio_duration]`, (d) the `schema_version` field is present and equals the pinned value. (Real-audio harness; `#[ignore]` if the recording is absent.)
- [ ] 1.2 Write failing test `child_binary_consumes_segmentation_model` — swap the model in the child's `model_dir` for a COMMITTED dummy fixture; assert the child's behavior changes deterministically (construction error or distinct/empty segmentation output — not a flaky live-filesystem mutation). Red test for the "pyannote segmentation model is actually consumed" requirement.
- [ ] 1.3 Write failing test `child_binary_smooths_at_pyannote_defaults` — assert the child's smoothing config is median rad=3, min_on=0.3s, max_off=0.5s at onset 0.5 (the only Phase 2b config that hit BOTH anchors). Inspect via a `--dump-config` flag or the stderr progress log.
- [ ] 1.4 Write failing test `child_binary_silent_audio_emits_empty` — silent/empty audio fixture → child emits `{"boundaries": [], ...}` and exits 0 (no crash).
- [ ] 1.5 Write failing test `child_binary_exit_codes` — assert exit 0 on success, 2 on model missing, 3 on decode failure, 4 on inference failure. (Drive each via a malformed input or `--crash-test` dev flag.)
- [ ] 1.6 Implement the production child by PORTING from `pyannote_ort_probe.rs` (Phase 2b) into a new crate modeled on `embed-probe-ort`: (a) the IPC contract (JSON-over-stdio per design D2, including the `schema_version: u32` field), (b) pyannote segmentation — load `pyannote-segmentation-3.0` via `ort::Session::commit_from_file`, slide a 10s window at 1s step, decode per-frame powerset logits to 3-speaker multilabel activity via hysteresis at onset 0.5, (c) pyannote smoothing ported from the probe (median rad=3 / on=0.3s / off=0.5s / onset 0.5), (d) uniform shed-to-cap (D5) before emit, (e) the exit-code contract. Reuse the `ort` session-builder pattern + Symphonia decode from `embed-probe-ort/src/main.rs`. Make 1.1–1.5 pass.

## 2. Parent integration + fallback (red→green)

- [ ] 2.1 Write failing test `parent_invokes_child_and_intersects_boundaries` — at `commands.rs:413-432`, when the child succeeds, the parent INTERSECTS its boundary set with the Whisper `transcript_segments` (`fetch_transcript_timestamps`) — pyannote boundaries inside a Whisper speech region are kept; Whisper silence regions are preserved as silence — and passes the INTERSECTED set as `transcript_segments` to `adapter.process()` (mock the child or use a fixture binary that emits a known boundary set; assert `process()` receives exactly the intersected set, not the raw child set).
- [ ] 2.2 Write failing test `parent_falls_back_on_child_crash` — simulate child crash (non-zero exit) via a `--crash-test` flag or a mock; assert the parent logs the failure, falls back to the existing chunk-grid (transcript-timestamp) boundaries as `transcript_segments`, AND the fallback path produces ≥1 labeled `SpeakerSegment` from `adapter.process()` on that fallback boundary vec (a silent zero-segment fallback is a regression, not a graceful degradation).
- [ ] 2.3 Write failing test `parent_falls_back_on_child_timeout` — child exceeds the configurable timeout (default 2× the cde5c264 budget); parent kills the child, falls back to chunk-grid boundaries, AND `adapter.process()` on the fallback boundary vec produces ≥1 labeled `SpeakerSegment`.
- [ ] 2.4 Write failing test `parent_falls_back_on_schema_mismatch` — child emits unparseable JSON, a missing required field, a wrong `schema_version`, or NaN-bearing boundary values; parent falls back (defensive parser, unknown fields ignored, missing/mismatched fields → fallback, NaN → fallback) AND `adapter.process()` on the fallback boundary vec produces ≥1 labeled `SpeakerSegment`.
- [ ] 2.5 Write failing test `boundary_oracle_synthetic_fixture` (UNCONDITIONAL — NOT `#[ignore]`) — commit a small synthetic multi-speaker WAV fixture with a KNOWN in-span turn (a deterministic, hand-constructed clip where the turn time is known a priori); assert the boundary exists in the child's emitted set. Runs in every CI build. (Distinct from the cde5c264 real-audio oracle below.)
- [ ] 2.6 Write failing test `boundary_oracle_cde5c264` (`#[ignore]` unless the on-disk recording is available) — on the real cde5c264 recording, assert a SPECIFIC boundary exists in the child's emitted set AND is absent in the chunk-grid-only baseline, for TWO windows: (a) the Ricardo interjection at ≈46:58 and (b) the actual complaint window 5.7–32.5 s. Assert the specific boundary, not a window-wide count delta. (Full-fidelity validation; the synthetic fixture above is the unconditional gate.)
- [ ] 2.7 Write failing test `persistence_oracle_cde5c264` — end-to-end (Part B on top of Part A): strictly MORE speaker-split rows persisted for the complaint window than the chunk-grid baseline.
- [ ] 2.8 Write failing test `single_speaker_not_fragmented` — a single-speaker fixture yields exactly one cluster after AHC+smoothing (no spurious second cluster from the finer candidate boundaries).
- [ ] 2.9 Write failing test `parent_emits_boundary_source_marker` — assert the parent emits a `boundary_source` log marker on EVERY diarization run: `boundary_source = "pyannote"` on the success path (2.1), and `boundary_source = "chunk-grid-fallback"` on each fallback path (2.2 crash, 2.3 timeout, 2.4 schema mismatch). This makes the "subprocess-sourced" requirement falsifiable even when the fallback fires — assert the marker value matches the path under test.
- [ ] 2.10 Write failing tests for parent-side boundary hygiene (M5): `parent_sorts_out_of_order_boundaries` (child emits non-monotonic boundaries; parent sorts before `process()`), `parent_clamps_out_of_range_boundaries` (boundaries beyond `duration_secs` clamped to `[0, duration_secs]`), `parent_falls_back_on_nan_output` (corrupted-model NaN at exit 0 → strict parse detects NaN → fallback), `child_killed_on_parent_drop` (parent's `tokio::process::Child` is killed via `Command::kill` on drop when the parent is killed mid-inference).
- [ ] 2.11 Implement the parent spawn+parse helper BEFORE the `spawn_blocking` closure at `commands.rs:413-432`: spawn child via `tokio::process::Command` (so the inference wait does NOT pin a blocking thread), parse JSON stdout into `Vec<(f64, f64)>`, validate `schema_version` + strict numeric parsing (no NaN, no coercion), sort + clamp the boundaries, INTERSECT with the Whisper `transcript_segments`, emit the `boundary_source` marker, fallback-to-chunk-grid on any failure. Make 2.1–2.10 pass.

## 3. Cap enforcement + shedding (red→green)

- [ ] 3.1 Write failing test `child_sheds_to_chunk_cap` — candidate-boundary counts at exactly `MAX_DIARIZATION_CHUNKS` (no shed) and at 601 (shed by one) both yield a child-emitted boundary count ≤ the cap; a stress fixture at ≈10× cap sheds without OOM.
- [ ] 3.2 Write failing test `child_uniform_shed_recovers_turns` — the LOAD-BEARING hypothesis test. A long (≥45 min, or a synthetic fixture scaled past the cap) two-speaker meeting with a rapid-alternation region and a comparable monologue region: after the child's uniform shed + parent's AHC + temporal-coherence smoothing, the alternation region recovers ≥80% of its within-region turns. If this cannot be made green with uniform shedding, the fallback is embedding-delta-weighted shedding (keep high-delta boundaries preferentially) — re-open the design at that point.
- [ ] 3.3 Write failing test `child_adversarial_audio_no_collapse` — noise/music that triggers many false change-points does not collapse the meeting to one speaker (AHC+smoothing rejects the noise fragments) nor fragment a true single-speaker meeting.
- [ ] 3.4 Write failing test `child_single_speaker_at_cap` — a long single-speaker meeting whose candidate boundaries hit the cap: AHC+smoothing re-coalesces all surviving fragments back to one speaker.
- [ ] 3.5 Implement uniform shed-to-cap in the child (D5): when the candidate-boundary count exceeds `MAX_DIARIZATION_CHUNKS`, shed every k-th boundary by position BEFORE emitting; merge sub-`MIN_SPEECH_SECS` survivors into their time-neighbor. Remove `build_chunks`'s `effective_split` uniform-grid subdivision (`sherpa_adapter.rs:331`) so the cap is enforced once. Make 3.1–3.4 pass.

## 4. Packaging + signing (release gate)

- [ ] 4.1 Declare the child binary via `bundle.externalBin` in `tauri.conf.json` (entry `binaries/pyannote-boundaries`, mirroring the existing `binaries/llama-helper` and `binaries/ffmpeg` entries). Add the `-$TARGET_TRIPLE`-suffixed build to `.github/workflows/build.yml`, copying the `build.yml:508-561` `llama-helper` pattern verbatim.
- [ ] 4.2 Reuse the `resolve_helper_binary` pattern at `sidecar.rs:108-227` verbatim to resolve the child at runtime (env-var override → relative-to-exe → `RESOURCE_DIR` fuzzy match, keyed off the `-$TARGET_TRIPLE`-suffixed name) — do NOT write a new resolver. Signing is AUTOMATIC: Tauri applies the existing `windows.signCommand` (`sign-windows.ps1` with DigiCert `smctl`) to every `externalBin` entry, as it does for `llama-helper`. Document in the release runbook that the sidecar is part of every signed release (no separate signing step).
- [ ] 4.3 Release gate (CI Windows runner): on a CI Windows runner (`windows-latest`), run the signed child via the real NSIS-installed path; assert exit-0 + valid JSON matching the pinned `schema_version` within the documented budget. (Non-deterministic Defender reputation cannot be CI-gated — drop "no quarantine on a clean Windows machine"; the "no quarantine" property is a release note, not a test assertion. A Defender scan/quarantine stall routes to the silent fallback, surfaced by the `boundary_source = "chunk-grid-fallback"` marker.)

## 5. Performance + reconciliation

- [ ] 5.1 Write failing perf test `subprocess_cde5c264_within_budget` — the full subprocess flow (spawn + decode + inference + IPC + parent AHC + refine_pass2) on cde5c264 completes within the `process()` time budget. If over budget, investigate Open Question 1 (temp-file audio passing to eliminate the re-decode). (`#[ignore]` unless the on-disk recording is available.) NOTE: Open Q1 (temp-file audio passing) is a REQUIRED decision for long meetings (8-hour upper bound projects to ~11.5h of inference — likely unacceptable). The temp-file path eliminates the re-decode AND lets the child stream windows without holding the full decode in the parent; the perf gate is the decision point at which temp-file passing flips from optional to required. Implement temp-file passing for meetings above the threshold this gate establishes BEFORE shipping for long meetings.
- [ ] 5.2 Run the existing diarization oracle tests on cde5c264 (label-quality, temporal-coherence, refine_pass2) with the subprocess enabled; assert no regression in label quality. (Label quality is preserved by construction — sherpa's extractor is untouched — but the test confirms end-to-end behavior.)

## 6. Pre-archive gate

- [ ] 6.1 Run `cargo test` (incl. `-- --ignored` for the cde5c264 boundary oracle and perf gate if the recording is available), `pytest backend/`, `pnpm test`, `pnpm lint` — all green. (No smoke test required: this change affects boundary placement in the diarization pass, not a new user-visible frontend behavior beyond what the companion change's smoke already covers.)
- [ ] 6.2 Re-read `openspec/specs/speaker-diarization/spec.md` + this change's `design.md`; amend the delta if implementation evolved (Open Question 1 — temp-file audio passing, now a required decision for long meetings; Open Question 3 — two-pass smart decimation, deferred). Open Question 2 (packaging) is RESOLVED as `externalBin` — verify the implementation matches.
- [ ] 6.3 `/opsx:archive` after spec/design reconciliation.
