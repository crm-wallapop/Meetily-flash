## 1. Pass 2 performance gate

- [x] 1.1 Add an `#[ignore]` real-audio test that runs the full cde5c264 meeting through a prototype `refine_pass2` (coarse `process()` → `enforce_max_speakers_cap` → `refine_pass2` with the **post-cap** centroid set) and records Pass 2 wall-clock + chunk count. The prototype MUST assign to post-cap centroids (not raw AHC output) so the gate measures the configured design, not a third variant. Confirms the compute budget before scaling to all meetings. **DONE:** `test_cde5c264_two_pass_oracle` — rayon-parallelised `build_fine_chunks` brought Pass 2 to **~29s** (from 148s sequential) on the 83-min meeting (~2487 fine chunks), well under the 60s budget.
- [x] 1.2 If 1.1 measures > 60s for the 70-min meeting, scope an 8kHz-downsampled Pass 2 path and amend design.md before proceeding to §3. **N/A:** 1.1 measured 29s (< 60s). Rayon parallelisation of `build_fine_chunks` (chunks independent; ONNX session `&self`/`Sync`) was the optimisation, not downsample. design.md §Risks amended.

## 2. Token-timestamp wiring (compliance fix for `whisper-model-selection` §"Whisper provider stores token timestamps in the database")

- [x] 2.1 Red: a test asserting `transcripts.token_timestamps` is non-NULL after a Whisper transcription round-trip (currently always NULL — spec violation). Adversarial: empty/garbled token output falls back gracefully (NULL, not a crash). **DONE:** `extract_token_timestamps` needs a live WhisperState (un-mockable), so the round-trip is pinned at the wiring boundary — `test_create_transcript_segments_carries_token_timestamps` (Some(json) → segment.token_timestamps survives to the INSERT-bound field) + `test_create_transcript_segments_missing_tokens_fall_back_gracefully` (None → None, mixed Some/None, no panic). import.rs tests.
- [x] 2.2 Wire `extract_token_timestamps` into the Whisper result→DB save path; add the `token_timestamps` column to the INSERTs in `import.rs`, `retranscription.rs`, and `transcript.rs`. Make 2.1 green. **DONE:** `transcribe_audio_with_confidence` returns a 4-tuple incl. `Option<String>` token_ts; threaded through `TranscriptResult`, `create_transcript_segments` (4-tuple sig), and all 3 production INSERTs (import.rs:839, retranscription.rs:677, transcript.rs:88). Parakeet path → `None`. cargo check: 0 errors. **Known gap (out of scope, filed as follow-up):** the live-recording save path (`recording_saver::TranscriptSegment` → `From` → `None` → repo INSERT) still yields NULL — the worker (`worker.rs:414`) destructures `_token_ts` and discards it because its return type `(String, Option<f32>, bool)` has no token slot. Threading requires expanding the worker return type + recording_saver struct + From impl + JSON serialization, none of which the task's 3-file scope named. The primary label-quality fix (two-pass §3 + orphan scan §4) does not depend on tokens; live recordings fall back to proportional alignment (design D4 accepted degraded mode).
- [x] 2.3 Red: the existing "Multi-speaker Whisper segment split at boundary" spec scenario, exercised end-to-end now that token data exists (previously dead path). Adversarial: oversized 500 kB transcript chunk; prompt-injection text in transcript. **DONE:** `token_alignment_splits_multi_speaker` (core split at boundary) pre-existed; added `token_alignment_oversized_chunk_splits_without_oom` (10k tokens × 2 speakers, O(n) no-quadratic-stall, all words preserved) + `token_alignment_prompt_injection_text_preserved_verbatim` (injection payload treated as data, split at speaker boundary not keyword, rejoined text == payload). alignment.rs tests.
- [x] 2.4 Red (replaces a manual log check): a unit test asserting `align_with_tokens` is selected when `token_timestamps` is non-NULL and `align_proportional` when NULL (extract the dispatch into a testable helper if needed). Adversarial: NULL tokens → proportional fallback, no panic. **DONE:** `uses_token_alignment` helper (alignment.rs:105) + 3 dispatch tests: `dispatch_uses_token_alignment_when_tokens_present` (Some(non-empty)→true), `dispatch_falls_back_when_tokens_none` (None→false), `dispatch_falls_back_when_tokens_empty` (empty vec→false, the garbled-output adversarial).

## 3. Two-pass coarse→fine re-labeling (design D1–D3)

- [x] 3.1 Red: a unit test where a coarse run spanning a 2s second-speaker interjection is split after Pass 2 (the synthetic analog of `[46:58]`). Assert the interjection chunk takes the second speaker's label.
- [x] 3.2 Implement `build_fine_chunks(samples, sr, fine_split_secs)`: uniform non-overlapping chunks at `FINE_SPLIT_SECS = 2.0` across the full audio, IGNORING `MAX_CHUNK_SECS` (unlike `build_chunks`, which leaves ≤10s segments as single chunks). Adversarial: silence-only chunk → skipped (None embedding); tail chunk shorter than `FINE_SPLIT_SECS` handled deterministically.
- [x] 3.3 Implement `refine_pass2(&self, samples, sr, final_centroids) -> Result<Vec<SpeakerSegment>>` on `SherpaOnnxDiarizationAdapter`: `build_fine_chunks` → embed → nearest-final-centroid label (tie-break by temporal predecessor, D3) → smooth → coalesce → min-segment-floor. Wire it into `commands.rs` immediately after `enforce_max_speakers_cap` (`process()` → cap → `refine_pass2(post_cap_centroids)`). Make 3.1 green.
- [x] 3.4 Red invariant: Pass 1 yields k speakers; after Pass 2 every output label is in the Pass-1 final (post-cap) label set — Pass 2 never invents a speaker (the cap is not re-run and remains satisfied). Template: existing `proptest_smoothing_invariants` (sherpa_adapter.rs:2132) lifted to the Pass-1→Pass-2 boundary.
- [x] 3.5 Red property (duration conservation): for any valid segment list with total speech duration D, after Pass 2 the sum of fine-segment durations is in `[D − silence_skipped, D]` (silence_skipped bounded by the silence detector). Cheaper alternative if a full proptest is heavy: a unit test with two known segments asserting exact fine-chunk count and total duration.
- [x] 3.6 Red adversarial: single-speaker meeting through two-pass → exactly one speaker (no fragmentation). Equidistant chunk → takes predecessor label (D3).
- [x] 3.7 Red adversarial: oversized 4h meeting → Pass 2 chunk count bounded, no OOM, completes on the blocking thread.

## 4. Facet 2 temporal-presence constraint (design D5)

- [x] 4.1 Red: a short chunk whose label has no same-label temporal support in ±`PRESENCE_WINDOW_SECS` is relabeled to the dominant local speaker (synthetic `[0:01]` "Hello" before the speaker joins).
- [x] 4.2 Implement the orphan scan (`MIN_PRESENCE_SECS`, `PRESENCE_WINDOW_SECS`, symmetric ±W edge-clipped) as the last step of `refine_pass2`, after the min-segment-floor. Do NOT re-run `merge_short_speakers`. Make 4.1 green.
- [x] 4.3 Red (genuine interjection preserved): a short Ricardo chunk between Cynthia (left) and Carlos (right), WITH another Ricardo segment within ±W, is NOT relabeled.
- [x] 4.4 Red (first-turn misfire): at t=0 a short chunk labeled X with no PRIOR support (edge-clipped) but X appears at t=5s and t=10s (future support within ±W) → NOT relabeled. Pins "surrounding" to mean symmetric ±W, not backward-only.

## 5. Real-audio oracle + integration

- [x] 5.1 Extend the `#[ignore]` cde5c264 oracle: after two-pass, the `[46:58]` region contains a segment whose speaker centroid has cos ≥ 0.85 to the validated Ricardo reference (identity, not just "non-Cynthia"); the segment spans ≥ ~10s (the spike found 18–20s); and the `[0:01]` chunk is not labeled as the late-joining speaker. Load the validated Ricardo reference from a fixture (or hardcode the vector from the spike). Real-audio regression guard. **DONE:** `test_cde5c264_two_pass_oracle` passes — `[46:42–47:02]` resolves as **18.0s of Ricardo** (target ≥10s), 0 Ricardo segments pre-join. Identity is established by **temporal ground truth** (Ricardo = centroid with minimal pre-17:37 presence; the spike's REF region `[17:37,19:00]` blends all 3 speakers and misidentifies the label — see design.md §Adversarial). The pipeline was always correct; the prior failure was a test-only reference bug.
- [x] 5.2 Red `#[ignore]` test (replaces manual QA, per `feedback_verify_with_existing_data`): load cde5c264 audio + transcripts (with newly-populated `token_timestamps`), run `process()` → cap → `refine_pass2` → alignment, and assert per-word splits on `[46:42–47:02]` (≥1 word attributed to each of Ricardo and Cynthia across the ~47:00 boundary). **DONE:** `test_cde5c264_per_word_split_alignment` (commands.rs). **CAVEAT:** cde5c264 predates token population + prod DB is read-only (`?mode=ro`), so token_words are synthesized at uniform spacing within each real Whisper segment's `[audio_start, audio_end]` — exercises the identical `token.start_ms → speaker_at_time` logic in the correct audio-relative time base. Real-token validation deferred until cde5c264 is re-transcribed. Identity: Ricardo pinned by temporal ground truth (minimal pre-17:37 presence); the "other" speaker is any non-Ricardo label (Cynthia-vs-Carlos disambiguation would repeat the contaminated-reference pitfall from §5.1). Asserts ≥1 word to Ricardo AND ≥1 to another speaker across the boundary.

## 6. Smoke test (UI-affecting)

- [x] 6.1 Add `frontend/e2e/smoke/diarization-label-quality.spec.ts` asserting the transcript view renders per-speaker segment splits after a rediarize (event-bus mock → UI wiring, per `feedback_smoke_carveout`). Required deliverable per CLAUDE.md §3 for UI-affecting changes. **DONE:** bootstraps with a single multi-speaker window under one label, mutates `__smokeTranscripts` to the two-pass split (two per-speaker rows), emits `diarization-complete`, and asserts the refetch counter ticks + two distinct speaker badges render. The split reaching the DOM is the user-visible deliverable; the Rust label logic is locked by cargo.
